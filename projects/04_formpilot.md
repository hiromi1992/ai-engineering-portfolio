# FormPilot

**現在の状態：状態管理、処理キュー、認証、観測基盤を実装済み。自動実行を段階的に検証中で、実フォーム送信は未実装です。**

## 何を作っているか

Webフォーム業務の自動化に向けたWindowsデスクトップアプリです。

外部へ送信する処理は、失敗時に二重送信などの影響が出ます。そのため、送信機能を先に作るのではなく、**処理状態、失敗理由、再試行可否、復旧方法を確認できる基盤**から整備しています。

## 実装済みの主な基盤

- Electron + Reactのデスクトップ構成
- 制限したIPC Bridge
- SQLiteでのCampaign / Target保存とMigration
- 中断から復旧できる処理キュー
- PostgreSQLを使ったWorkspace認証・Role管理
- Sender / Template / Schedule / Pacing / Test mode設定
- 実行イベントと失敗理由の記録
- Typecheck / Lint / Unit Test / Build / Desktop検証

## 主な設計

- 「まだ開始していない」「実行中」「送信した可能性がある」「完了」を分けて扱う
- 送信した可能性がある場合は、自動で同じ処理を繰り返さない
- 再試行してよい失敗と、人の確認が必要な失敗を分ける
- 外部操作を追加する前に、状態と失敗理由を観測できるようにする

## 検証状況

非公開の現行開発リポジトリでは **20 test files / 69 test cases** を確認しています。

処理キュー、復旧、認証、データ保存、UI、実行状況の観測など、実フォーム送信の前段を中心に検証しています。

## 自分の担当

- 製品要件と開発段階の設計
- 状態、保存、権限要件の整理
- Coding Agentへの実装指示
- Migration、Recovery、E2Eのテスト観点設計
- CI・回帰確認
- 開発範囲の管理
- 受入・再設計判断

コード生成・直接実装は主にCoding Agentへ委譲しています。

## 今後

- Deterministic Form Engineの実行機能
- 実フォーム送信
- 実測データを見たうえでのAI Browser / AI Fallback採用判断
